upstream frontend {
    server frontend:5173;
}

# dev mode
# server {
#     listen 80;

#     server_name jayapi.nekoth.com;

#     # frontend (React app)
#     location / {
#         proxy_pass http://localhost:5173;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     }

#     # backend (API)
#     location /api/ {
#         proxy_pass http://localhost:5050/;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     }

#     # content center
#     location /content/ {
#         proxy_pass http://localhost:5050/content/;
#         proxy_set_header Host $host;
#     }
# }


# production
server {
    listen 80;
    server_name blogone.nekoth.com;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri /index.html;
    }
}

server {
    listen 80;
    server_name jayapi.nekoth.com;

    location / {
        proxy_pass http://backend:5050/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    location /content/ {
        proxy_pass http://backend:5050/content/;
        proxy_set_header Host $host;
    }
}